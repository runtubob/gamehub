kind = "web"
previewPath = "/"
title = "GameHub - PS Rental"
version = "1.0.0"
id = "artifacts/gamehub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19931

[services.development]
run = "pnpm --filter @workspace/gamehub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gamehub", "run", "build" ]
publicDir = "artifacts/gamehub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19931"
BASE_PATH = "/"
